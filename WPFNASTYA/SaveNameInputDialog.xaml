<Window x:Class="CardGame.GUI.SaveNameInputDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Сохранение игры" 
        Height="200" 
        
        Width="400"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">

    <Grid Background="#1A1A1A" Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Введите название сохранения:" 
                   Foreground="White"
                   FontSize="16"
                   Margin="0,0,0,10"/>

        <TextBox x:Name="SaveNameTextBox"
                 Grid.Row="1"
                 Background="#333"
                 Foreground="White"
                 BorderBrush="#555"
                 FontSize="14"
                 Padding="5"
                 MaxLength="50"/>

        <StackPanel Grid.Row="3" 
                    Orientation="Horizontal" 
                    HorizontalAlignment="Right"
                    Margin="0,20,0,0">
            <Button Content="Отмена" 
                    Width="80" Height="30"
                    Background="#555"
                    Foreground="White"
                    Click="CancelButton_Click"/>
            <Button Content="Сохранить" 
                    Width="80" Height="30"
                    Background="#B86F2F"
                    Foreground="White"
                    Margin="10,0,0,0"
                    Click="SaveButton_Click"/>
        </StackPanel>
    </Grid>
</Window>